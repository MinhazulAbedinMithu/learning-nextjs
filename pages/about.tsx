import React from "react";
import { Layout, Meta } from "../components";

const about = () => {
	return (
		<Layout meta={<Meta title="About" description="About Learning Next JS" />}>
			<h2>About</h2>
		</Layout>
	);
};

export default about;

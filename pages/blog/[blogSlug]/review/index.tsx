import React from "react";
import { Layout, Meta } from "../../../../components";

const Review = () => {
	return (
		<Layout meta={<Meta title={`Blog - Review`}/>}>
			<h2>All Review</h2>
		</Layout>
	);
};

export default Review;
